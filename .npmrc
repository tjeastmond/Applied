# Keep pnpm lifecycle banners off stdout so `pnpm applied:agent ... --json` pipes valid JSON.
reporter=silent

only-built-dependencies[]=better-sqlite3
only-built-dependencies[]=esbuild
only-built-dependencies[]=sharp
